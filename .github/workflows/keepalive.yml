name: Keepalive Render

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -s https://edusaas-api-tbig.onrender.com/health
          echo "Ping enviado em $(date)"
